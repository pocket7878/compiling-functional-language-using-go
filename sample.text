defn add x y = { x + y }
defn double x = { add x x }
defn main = { double 163 }
data List = { Nil, Cons Int List }
defn length l = {
    case l of {
        Nil -> { 0 }
        Cons x xs -> { 1 + length xs }
    }
}
